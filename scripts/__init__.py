"""Probe and loop scripts (LLM design loop, hazard curves, character induction).

Made a package so experiment runners and tests can import these by path
(`from scripts.hazard_curves import ...`) after an editable install.
"""
